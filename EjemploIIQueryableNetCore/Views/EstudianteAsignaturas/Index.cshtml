@model IEnumerable<EjemploIIQueryableNetCore.Models.Negocio.EstudianteAsignatura>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Nota)
                </th>
            <th>
                @Html.DisplayNameFor(model => model.Asignatura)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Estudiante)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nota)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Asignatura.IdAsignatura)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Estudiante.IdEstudiante)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdEstudianteAsignatura">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdEstudianteAsignatura">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdEstudianteAsignatura">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
